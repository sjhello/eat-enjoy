pipeline {
    agent any

    environment {
        PATH = "/usr/lib/gradle/bin:$PATH"
        // PATH = "/opt/gradle/gradle-6.6.1/bin:$PATH"
    }

    stages {
        stage('Build') {
            steps {
                echo 'building!!'
            }
        }
    }
}
